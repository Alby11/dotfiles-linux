# vim: filetype=zsh

if CHECK_COMMANDS bw; then
  export BW_CLIENTID="user.a4e9024d-c7ea-4088-84eb-ae4600a2faa6"
  export BW_CLIENTSECRET="5ZZFAwuXOQqe92P4W2J0zuGPlo08CR"
  export BW_PASSWORD="/16P1r137R31s23."
  zsh <(bw unlock --passwordenv BW_PASSWORD | grep --color=auto 'export BW_SESSION' | sed -s 's/^$ //')
  export BW_SESSION="$(bw unlock --passwordenv BW_PASSWORD | \
    grep --color=auto 'export BW_SESSION' | \
    sed -s 's/^$ //' | \
    sed -s 's/export BW_SESSION=\"//' \
    | sed -s 's/"$//')"
fi
