# vim: filetype=zsh
remmina_session_save() {
  rsync -av ${HOME}/.config/remmina/work_* ${HOME}/gitdepot/profileFiles/remmina
  local curdir=$(pwd)
  cd ${HOME}/gitdepot/profileFiles
  git add remmina/work_* 
  git commit -m 'add/edit session'
  git push
  cd $curdir
}
